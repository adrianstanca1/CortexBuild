# Shared Packages

| Package | Purpose |
| --- | --- |
| `types` | Domain models, API contracts, event payloads |
| `ui` | Component primitives, theming, icons |
| `automation` | Workflow DSL, agent definitions, client SDK |
| `utils` | Cross-cutting helpers (logger, feature flags) |

## Setup TODO
- Initialize `package.json` with workspace settings.
- Configure TypeScript project references for faster builds.
- Publish documentation via Storybook or Ladle for UI package.
